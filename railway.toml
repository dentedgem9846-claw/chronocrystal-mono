[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/simplex/docker/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
